# a tuples is created by placing all the elements inside parenthess().
# Tuples are immutable. that means the elemets of a tuple cannot be changed once it has been assigned.


my_tuple= (12, 14 , 25)
print(my_tuple)
print(sum(my_tuple))